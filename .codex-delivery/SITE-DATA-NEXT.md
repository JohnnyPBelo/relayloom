# Dados declarativos das páginas — integração em validação

## Estado de retoma — 18 de Setembro

**Gate local concluído e recolhido:** sessão39421 terminou0, `contrast-final` PASS com fontes estáveis.138Node,Go sites/app comrace,11interop,4UI Node+4UI Go e36porbrowser(108).15repetições dirigidas deFirefox. O curador exportou84artefactos (~2,6MB) para `docs/evidence/site-data`, verificando hashes/contagens e conservandoFAILanteriores; o primeiro exportador falhou apenas ao procurar o log ausente de um typecheck sem saída, corrigido para respeitar hashnullsem inventar bytes. README/relatório/manifesto distinguem escopo e limitações. Todos os handles anteriores foram recolhidos. Próximo passo: commit delimitado, integração no principal, documentação actualizada e gate público/HTTPS. Tabelas ainda não publicadas.

**Operação prioritária actual: sessão 39421, gate `contrast-final`.** O `after-focus` terminou com 4 UI Node + 4 UI Go e 36 Chromium + 36 Firefox aprovados; WebKit teve 35 PASS e 1 FAIL de contraste no botão `.primary`. O degradê tinha cor base transparente. O controlo adicional falhou antes da alteração (`.cache/site-data-contrast-before`); `liquid-glass.css` usa agora a base opaca `#11574f` sob o mesmo degradê. O relatório Axe original está em `.cache/site-data-contrast-failure/preview-axe.json`. O build actual foi executado e guardado em `.cache/site-data-contrast-build.log`.

Comando actual no worktree: `PLAYWRIGHT_BROWSERS_PATH=/home/absint0o/projects/relayloom/.cache/playwright RELAYLOOM_SITE_DATA_EVIDENCE=.cache/site-data-gate/contrast-final node scripts/verify-site-data.mjs typecheck ui-node ui-native browser-chromium browser-firefox browser-webkit`. Recolher o handle e o relatório antes de editar fontes ou repetir. O curador `.cache/curate-site-data.mjs` foi adaptado para conservar também o FAIL de contraste e exigir a matriz final aprovada. Não foi ainda executado. As descrições seguintes são históricas quando indicam outro handle activo.

**Actualização de 18 de Setembro, prioritária:** o gate `refined` terminou PASS, com 138 Node, 4 UI Node + 4 UI Go e 36 cenários por engine (108), fontes inalteradas. O gate `final-interop` voltou a passar typecheck e 11 casos na fonte actual. O suplemento de tema escuro passou no Chromium, mas expôs uma falha no Firefox depois de guardar: o campo inválido regressava a1.25. Trace preservado em `.cache/site-data-gate/supplement/firefox-artifacts`. O controlo determinístico em `site-data-component.spec.ts` mostrou que a reposição de foco por requestAnimationFrame retirava o foco de uma nova célula escolhida sem pointerdown/keydown. Falhou antes (`.cache/site-data-save-focus-before`); a correcção em `studio.tsx` respeita também input/focusin e nunca foca o botão quando há outro elemento activo. Três repetições de5percursosFirefox passaram (15PASS), incluindo valor inválido preservado.

**Operação agora em curso: sessão24712**, `PLAYWRIGHT_BROWSERS_PATH=/home/absint0o/projects/relayloom/.cache/playwright RELAYLOOM_SITE_DATA_EVIDENCE=.cache/site-data-gate/after-focus node scripts/verify-site-data.mjs typecheck ui-node ui-native browser-chromium browser-firefox browser-webkit`. Recolher esse handle e relatório, não a antiga6382 já terminada. A alteração de foco obriga a conservar a matrizrefined como evidência anterior e usar esta nova matriz para aUI final. Node/Go/interoperabilidade não mudaram nessa correcção. O build executado antes das15repetições ficou em `.cache/site-data-focus-build.log`.

As últimas fontes de teste acrescentam tema escuro/Axe, dimensões reais do diálogo e o novo controlo de foco. O editor usa uiHost, pelo que também pode testar o HTTPS com `RELAYLOOM_LAUNCH_URL`. O gate público/config inclui agora os4percursos do editor e o manifesto inclui osfixtures. Ainda não foi executado/publicado esse gate público. Depois do gate actual, `.cache/curate-site-data.mjs` prepara provas apenas se os relatórios/contagens/hashes forem válidos; inspeccionar o resultado antes de commit. README/STATUS principal ainda precisam da extensão, sem apagar WIP anterior. Nenhum commit oupush de tabelas foi feito.

Esta secção substitui as descrições de trabalho pendente do registo histórico abaixo. Só se trabalha em `/home/absint0o/projects/relayloom/.cache/site-data-next`, branch `codex/site-declarative-data`, base `6dd879d`. O principal está em `853d0e7`, com WIP anterior preservado. As tabelas ainda não foram commitadas, integradas no principal nem publicadas. A web continua na fonte `2d84a61`, distribuição `4bc293c5`.

Já implementado nesta árvore: documento v2 com blocos de tabela, validadores TS/Go, editor ligado ao estúdio, linhas/colunas/tipos, importação CSV/JSON com validação antes da substituição, exportação, confirmação de eliminação, desfazer, pesquisa/ordenação/paginação no leitor. O documento v1 mantém-se aceite; tabelas exigem v2 e clientes antigos recusam essa versão. Não há execução de scripts/HTML/SQL, nem interoperabilidade com ZeroNet. Assinatura, audiência e seeding mantêm os contratos existentes.

Resultados recolhidos antes do gate: typecheck pendente da sessão44562 terminou0; primeira UI integrada1PASS; importação falhou no nome acessível do selector de formato (evidência preservada em `.cache/site-data-import-failure`), corrigido sem aumentar prazos; UI integrada/importação2PASS. A revisão visual dessas capturas encontrou botões demasiado estreitos no móvel e um input de ficheiro indevidamente visível; foram corrigidos e acrescentadas assertivas geométricas. Capturas anteriores em `.cache/site-data-design-before`. Dados/catálogos/documento/i18n76PASS e dois percursos v2 Node→Go→Node/Go→Node→Go com rascunho cifrado, corrupção recusada e seeder reiniciado2PASS. Não confundir estes resultados dirigidos com a matriz final.

Gate actual: `PLAYWRIGHT_BROWSERS_PATH=/home/absint0o/projects/relayloom/.cache/playwright RELAYLOOM_SITE_DATA_EVIDENCE=.cache/site-data-gate/first node scripts/verify-site-data.mjs`, sessão40626. Recolher o handle e `.cache/site-data-gate/first/report.json`; não repetir etapas já aprovadas sem motivo. O driver é sequencial, regista comandos/saídas/hashes/fontes, pára na primeira falha e aceita nomes explícitos de fases para retoma em nova pasta. Um passe parcial nunca inclui fases não executadas. Não editar fontes enquanto este gate corre; documentação é independente.

O gate inclui todos os testes Node de sites/i18n, Go sites/app com race, todos os testes de interoperabilidade de sites, UI de páginas sobre Node e Go com `nativeRuntime` confirmado, e doze ficheiros afectados de páginas nos três browsers. Abrange versões1e2, dados exactos, RTC→WebSocket→TCP, pausa/heal e seeder único reiniciado; há um novo controlo de alteração de célula sem certificado. Os gates globais/publicação continuam necessários antes de entregar a distribuição.

No CI35276560255 da fonte2d84a61, todos os jobs terminaram: Node três hosts, Go, UI nativa, RNS, browsers e três pacotes desktopPASS; iOSFAIL. O artefacto de cerca de1MiB foi recolhido em `.cache/ios-35276560255`. O startup e a preparação da fotografia passaram; `execute-ui-test` terminou65 em299870ms. As capturas guardadas ainda mostram configuração inicial/teclado. O relatório sanitizado não estabelece a causa exacta. Não atribuir automaticamente esta falha ao picker anterior, não saltar fotografia nem aumentar prazos. Não houve alteração da bridge/serviços/permissões.

Próximo: recolher gate, corrigir falhas pela evidência, rever capturas finais; curar provas e docs/STATUS/README/traceability; commit apenas dos ficheiros desta extensão; integrar sem copiar worktree antiga nem alterar WIP alheio; gate de regressão e publicação/HTTPS exactos. Manter 15GiB livres (última leitura27GiB). A observação RTCFirefox anterior permanece aberta, e esta revisão é do agente principal, não independente.

Depois deste incremento, continuam obrigatórios: datasets/ficheiros opcionais P2P por referência assinada; contribuições de outros utilizadores com a sua própria assinatura e aprovação explícita do dono, formulários declarativos limitados e projecções locais; grupos web dinâmicos, backup/rotação/keystore, todos os meios/plataformas e testes físicos/revisão independente. Recuperação continua sequencial sem novos agentes, Astra/CopilotUltra intacto. Produto completo não concluído.

## Registo histórico — passos iniciais, não estado actual

O contrato integral inclui liberdade de sites inspirada no ZeroNet, sem scripts remotos. Este incremento prepara tabelas de dados que serão parte do snapshot assinado do proprietário; ordenar/pesquisar no leitor não altera os bytes publicados nem transfere autoria. Contribuições de terceiros continuam a exigir a sua própria assinatura e aprovação/consentimento do proprietário, não uma chave de leitura.

Código candidato neste worktree (`.cache/site-data-next`, branch codex/site-declarative-data, base6dd879d):

- `packages/content/src/site-data.ts`: parser de tabelas tipadas (texto, número, booleano, dataISO eHTTPS), consultas locais literais, ordenação determinística, nullsemprefinal e paginação. Recusa getters, listas esparsas, chaves extra, duplicados, URLs executáveis/com credenciais e valores fora dos limites. Limites iniciais:12colunas,256linhas,64KiBUTF-8 e1000caracteres/célula; continuam sujeitos ao orçamento total do documento quando integrados.
- `apps/web/src/site/table.tsx` eCSS: rendererReact com pesquisa, ordenação acessível, paginação e scroll por teclado, sem chamadas de rede ou execução de expressões. Apenas preparado; ainda não montado no site nem testado porbrowser.
- `apps/web/src/i18n/site-data.ts` e catálogo:14entradas novasPT/EN/ES.
- `tests/site-data.test.ts`: cinco casos com controlos positivos/negativos e verificação debytes/imutabilidade. Passaram em213,5ms. Juntamente comos4testesi18n, novePASS em1081ms. Typecheck do parser/componente passou; a alteração final do catálogo foi coberta por i18n, mas ainda não por um novo build completo.

Comandos nesta árvore: `node --import /home/absint0o/projects/relayloom/node_modules/tsx/dist/loader.mjs --test tests/site-data.test.ts`; depois `tests/i18n.test.ts tests/site-data.test.ts`. Typecheck: `node /home/absint0o/projects/relayloom/node_modules/typescript/bin/tsc --noEmit`. Dependências herdadas da árvore do mesmo projecto; não houve downloads nem alteração deconfigs. Logs em `.cache/site-data-first.log`,site-data-i18n.log,site-table-typecheck.log.

Ainda NÃO é funcionalidade entregue: falta protocolo de blocos/documento e validação Go; toolbar/inspector/edição decolunas/linhas, importaçãoCSV/JSON limitada eundo; assinatura/transporte/persistência interop; renderização/browserUI3engines, limitesdebytes e negativos; contribuição assinada/aprovação/datasets/ficheirosopcionais. Não contornar a versão do documento, o fallback ou o validador actual com campos extra silenciosos. Decidir a compatibilidade de versão antes de ligar o bloco. Preservar liberdade dentro de limites, semSQL/JavaScript/CSSarbitrários.

O principal mantém a candidata6dd879d congelada no gate público. Não copiar este worktree para cima dela nem apresentá-lo como parte dessa distribuição. A observaçãointermitenteRTC e todo orestantecontrato continuam abertos.


Port adicional: native/sites/data.go valida as tabelas com os mesmos limites e a políticaURLlexical já partilhada doestúdio. TestDataTableVectorWorker é umhelpertest-only e recebevectoresna cachedo projecto. tests/site-data-interop.test.ts usascripts/go.mjs e passou40vectores(17positivos/23negativos), incluindo datasbissextas,Unicode/surrogate,limitesnuméricos eURLs. Conjunto dados+i18n+interoppassou10testesem15,75s. Logs .cache/site-data-interop.log e site-data-vectors.json. CachesGo/toolchainsdesteworktree são symlinksparaacachedeste mesmo projecto, semdownloads adicionais. Não exportar environmentpaths como requisitos deexecução: o teste usaoscriptnormaldeGo.

O rendereragora escolhe17algarismos significativos para não mostrar uma fracção muito pequena comozero só por truncagemdecasasdecimais. Esta última alteração não tem ainda testevisual; semclaims deUI entregue.


O primeiro teste real do componente correu emChromium e passou(1,3s): pesquisa literal, ordenação numérica por teclado, paginação, valor1e-15visível, recusa de camposactivos, fonteinalterada e viewport390semoverflow. AxePASS. Captura .cache/site-data-component/chromium/table.png inspeccionada. Ainda não é aplicação/assinatura/editor integrados, nem Firefox/WebKit ou paridadedeficheirosopcionais. Não contar esta fixture como feature publicada.
