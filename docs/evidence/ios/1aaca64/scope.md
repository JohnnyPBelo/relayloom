# iOS1aaca64 — arranque real falhado antes da fotografia

CI34752751771, commit1aaca64c8eea188527a265db03d7f8490a7da111. Node3OS, Go completo e três jobs desktop passaram. iOS26.4.1/Xcode26.6: boot169.530s, build-for-testing39.092s, instalação13.165s passaram; execute-startup-test saiu65 em165.729s. Uma captura nativa foi exportada e revista por root: mostra “Não foi possível activar o isolamento da interface.”, com botão Tentar novamente. Esta mensagem é emitida no ramo de falha de compileContentRuleList, antes da construção da WKWebView. Não é prova de falha de arranque do núcleo Go.

Nenhuma fotografia foi importada e o teste funcional completo não executou. simulatorExecuted=false no relatório é a flag de sucesso do gate funcional, não uma negação do arranque real capturado. Não atribuir1 passe iOS nem alterar a falha. Apenas o simulador criado foi removido; nenhum download ou mudança de segurança/serviços. Logs do job original em .cache/ci-1aaca64-ios-job.txt.

A regra actual inclui `(/|$)`, com o marcador$ dentro de parênteses. A documentação WebKit exige$ como último carácter da expressão: https://webkit.org/blog/3476/content-blockers-first-look/. Esta incompatibilidade é uma hipótese concreta sustentada pelo código/contrato do parser; o artefacto não inclui o NSError exacto. Próxima alteração: separar origem exacta e prefixo com barra, preservando a política, e acrescentar controlos positivos/negativos com compilação WebKit real antes de repetir iOS. A correcção ainda não foi implementada neste registo; gate local de envio está a correr com fontes congeladas.
