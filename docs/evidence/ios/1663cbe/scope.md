# iOS1663cbe — isolamento e arranque confirmados; fluxo funcional incompleto

CI34754740869, Xcode26.6/iOS26.4.1. A política nova compilou no WebKit real; a expressão antiga foi recusada com WKErrorDomain:6.45 asserções de host passaram. Node3OS, Go completo e três desktop passaram.

Boot156.373s, build-for-testing39.163s, instalação12.299s passaram.1 XCTest de arranque passou (0 falhados/omitidos), comando33.838s. Importação da fotografia passou38.204s. O teste funcional executou e saiu65 em119.017s; não há resultado completo passado. Não houve timeout nesta etapa funcional. O resumo estruturado não foi consultado após o comando falhar, mas o log sanitizado foi exportado e identifica a falha. A primeira listagem de root omitiu *.log por respeitar .gitignore; isso foi corrigido, sem atribuir a omissão ao runner.

Root reviu as duas capturas nativas: ambas mostram o ecrã inicial renderizado, com o formulário a continuar abaixo da área visível. ui-01 é a captura de onboarding feita antes de preencher os campos; não é uma captura do momento da falha. O log confirma identity-created e a falha missing("post form completed") após tocar em Publicar:1 teste falhado74.523s, comando119.017s. O formulário não desapareceu em30s. A tentativa de dismissKeyboard encontrou teclado mas não registou toque em Done nem no título; a causa do submit não está demonstrada. Próxima alteração: procurar Done no conjunto nativo de botões, exigir teclado fechado e guardar captura de falha antes de terminar a app, preservando as mesmas asserções/prazos/isolamento. Não alterar produto com base numa hipótese ainda não isolada. A criação de identidade pela UI está confirmada pelo checkpoint; publicação/mensagem/anexo/recuperação completos continuam sem passe.

Os hashes dos ficheiros descarregados foram comparados com o manifesto do artefacto. Só o simulador criado foi removido, sem download ou alteração de serviços/segurança. Esta versão não contém as confirmações de grupo ainda em integração local. Nenhum teste em hardware iOS físico ou assinatura de distribuição foi executado.
