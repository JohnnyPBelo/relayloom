# Follow-up to CI f93e741

Run34669716466 passed Node on Linux/Windows/macOS, the full Go job (including the previously failing outbox mixed case) and Windows/macOS desktop packaging. Linux desktop execution and the new iOS simulator execution job failed. Earlier local results remain version/environment-specific.

Linux failed with `UnknownVizError` while capturing the loaded hidden renderer under Xvfb. Runtime authentication/isolation checks had already executed, but the job was still a failure. The dev smoke now maps only its owned test window without focusing it, requests repaint and obtains a nonempty frame from Electron's actual presentation callback. A15-second capture deadline remains; GPU, sandbox and web-security preferences are unchanged. The changed smoke passed on this Linux host, with the resulting native image inspected. Actual Xvfb CI recovery still requires the next run; Xvfb is not installed on this local host.

The Apple runner used installed iOS26.5(23F77), Xcode26.6(17F113) and one created iPhone SE3 simulator. At the180-second limit, `simctl bootstatus` still reported initial LaunchServices/CoreLocation data migration. The gate stopped and deleted exactly its owned simulator; the application/XCUITest had not run. The first-boot allowance is now600seconds for the same owned device, retaining the global20-minute supervisor deadline,15GiB reserve, one-device rule, logs and cleanup. No service, location setting, permission, signing account or runtime installation was changed. This bounded allowance is not an iOS success claim.

The runner's11 Linux host-only policy/fixture tests and syntax check passed after the change (1.139s for the tests); those tests do not execute Apple tooling. Sanitized first-attempt boot evidence is retained in `docs/evidence/ios/f93e741`. The next CI run must prove whether cold boot finishes and then compile/execute the actual XCUITest. If migration remains blocked, preserve that result instead of retrying the same attempt indefinitely.
