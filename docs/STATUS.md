# RelayLoom — estado verificável

**CI187d7f3 — run34778397733 terminado:** Linux/macOS Node e UI Linux passaram; o controlo offline corrigido passou em Windows, mas outro percurso de seeder falhou com ECONNRESET numa leitura de estado através de socket HTTP reutilizado. O artefacto confirmou processo vivo e ausência de headers. Uma pausa deliberada do cliente reproduziu este modo de falha localmente; uma pausa apenas do servidor não. O helper passa a fechar a ligação de cada RPC, mantendo uma tentativa por operação; o teste novo confirma publicação exactamente uma vez após o atraso.4 testes dirigidos passaram92.466s, incluindo percursos mistos; a nova execução CI ainda é necessária. Servidor/prazos/transportes não foram alterados por esta correcção. [Diagnóstico e evidência](evidence/group-carriers/http-fixture-lifecycle).

**CI63ded7b — run34777466884 terminado:** Node Linux/macOS e UI Linux passaram. Windows falhou na fixture de seeder, que exigia exitCode após um processo terminar por sinal; os jobs Go/desktop/iOS foram omitidos. A correcção337561c aceita os dois campos de terminação e exige recusa efectiva da porta TCP do autor, com controlo positivo anterior.6 testes afectados passaram41.055s em Linux, incluindo o ramo de terminação por sinal e os percursos Node/Go/TCP/serial; a nova execução Windows ainda é necessária. [Falha e controlo reforçado](evidence/group-carriers/windows-offline-control). O passe macOS deste run não identifica por si a causa do reset HTTP anterior.

**Carriers C1 — gate local concluído sobre adfd52a:** sincronização automática de headers/snapshots privados em Node/Go, com partição/heal, autor offline, seeder opaco, respostas recuperadas, replay após unlock e TCP→serial por PTY. Corrigidas e reproduzidas quota contornada por respostas, retirada prematura de provas e validação tardia de controlos públicos. Build6.056s;253 Node405.684s;161 Go principais/race711.420s (12 helpers pelos drivers);47 interoperabilidade480.894s;35 casos principais SQLite C283.498s;19 UI Node138.097s/19 Go128.687s. Desktop Linux preparação0.319s/execução2.412s/pacote descompactado7.260s/execução empacotada1.255s.62 Axe sem violações e326 fontes inalteradas. [Semântica](GROUP-CARRIERS.md) e [evidência](evidence/group-carriers/final). C2 (convites/consentimentos/saídas), C3 (UI dinâmica), plataformas actuais e revisão independente continuam pendentes.

**CI adfd52a — run34765195200 terminado:** Node Windows/Linux e UI Linux passaram; macOS falhou no commit de remoção após reiniciar o autor (`fetch failed`, group-event-seeding.ts). Go/desktop/iOS foram omitidos; o ajuste XCTest abaixo não foi executado em Apple neste run. [Evidência](evidence/ci-adfd52a). O helper HTTP local passou a isolar sockets por processo e guardar diagnóstico sem retries de mutações; um passe Linux não demonstra a causa nem a correcção macOS.

**Ajuste do XCTest iOS após5d24a16:** procura semântica de controlos, gestos limitados para o anchor e recuperação compatível com conversa compacta; mantém teclado oculto e todos os fluxos/asserts.22 testes host1.833s e estática0.030s passaram. Swift e execução Apple ainda por confirmar; sem passe funcional inferido. [Âmbito](evidence/ios/input-controls-host).

**Eventos de grupo — gate local concluído sobre93f24f1:** APIs Node/Go publicam reacções/comentários/edições com head/intersecção autorizados e eliminação histórica mínima pelo autor. Filas/inventário/request revalidam alvo/provas/época, incluindo sem outbox de mensagens. Novo leitor não recebe chaves antigas; removido não decifra edição nova mas recebe deleção histórica; seeder com autor desligado, reinício, falhas e corrupção testados. Uma regressão de dupla leitura de anexos foi corrigida mantendo o orçamento de memória. Build5.580s;236 Node291.635s;153 Go de topo/race621.017s (11 helpers pelos drivers);38 interoperabilidade378.001s;SQLite C207.489s;19 UI Node134.686s/19 Go133.737s.22 host iOS1.975s/estática0.029s. Desktop Linux build0.208s/execução1.878s/pacote4.889s/execução empacotada0.876s.56 Axe arquivados sem violações,300 fontes inalteradas. [Semântica](GROUP-EVENTS.md) e [evidência](evidence/group-events/final). Carriers, composição/gestão dinâmica e versões móveis actuais continuam pendentes.

**Liquid Glass — gate local concluído sobre f37067f:** navegação flutuante, temas, barra móvel, conversa compacta e paleta Ctrl/Cmd+K funcional. Pesquisa limitada a mensagens autorizadas carregadas; conteúdo eliminado excluído, consulta esquecida após bloqueio. Modo económico/contraste/preferências reduzem efeitos. Typecheck 3.959s, web 1.551s, CLI Go 0.138s; **19 E2E Node em142.211s e19 E2E Go em127.734s**, zero skips/flaky. Desktop Linux: build0.110s, execução1.694s, AppImage10.049s, execução do pacote descompactado0.768s. **62 relatórios Axe sem violações**,289 fontes inalteradas; fontes dos núcleos Node/Go/transporte/crypto não alteradas nesta iteração. Falhas de contraste/transparência, alvos móveis e acesso à outbox após reinício foram corrigidas e repetidas sem retirar cobertura. [Evidência](evidence/liquid-glass/final) e [âmbito](LIQUID-GLASS.md). Android/iOS actuais, hardware, leitor de ecrã manual e revisão independente desta UI permanecem pendentes.

**CI93f24f1 — run34760224339 terminado:** Node3OS, Go e três pacotes desktop passaram. iOS passou arranque63.211s e fotografia3.101s; o teste funcional falhou58.149s antes de criar identidade ao não encontrar o controlo de fechar teclado. A captura da falha mostra a barra acessória e o botão Criar identidade, não um resultado de submissão. Nenhum fluxo funcional completo passou. [Log/capturas e diagnóstico](evidence/ios/93f24f1).

**CI f37067f — run34757307906 terminado:** Node3OS, Go e três pacotes desktop passaram. O teste iOS de arranque passou (1 teste41.627s, comando94.457s). A importação de fotografia excedeu60.306s, antes do teste funcional; a alteração de Done/captura não foi exercitada. Nenhum fluxo funcional completo passou. [Relatório, logs e captura](evidence/ios/f37067f).

**Confirmações históricas de grupo — gate local concluído:** delivery automático e receipt por leitura nos dois motores, payload mínimo/audiência original e admissão antes de transmitir. Read completo mantém-se após close; outro envio realmente incompleto fica superseded. Bloqueio, falta de bytes/provas, rollback/resposta perdida e recibo mal ligado testados. Build5.598s;224 Node219.220s;148 testes Go de topo/race548.587s (11 helpers pelos drivers);33 interoperabilidade315.613s;SQLite C143.711s;17 UI Node118.995s e17 Go112.607s;22 host iOS2.319s/estática0.050s. Desktop Linux: build0.230s,execução1.011s,pacote5.055s,execução empacotada0.809s.26 Axe sem violações;284 fontes inalteradas. Evidência em docs/evidence/group-confirmations/final. Eventos, carriers, composição/gestão dinâmica, binários móveis actuais e revisão independente continuam pendentes. [Comportamento](GROUP-CONFIRMATIONS.md).

**Criação/envio de grupo sobre1aaca64 — gate local concluído:** APIs Node/Go criam texto/anexo/reply a partir do snapshot autenticado, sem contactos globais; admissão e intenção partilham commit antes de armazenamento/transmissão. Falhas, oito mortes reais e interoperabilidade Unicode/compatibilidade antiga verificadas. Build5.816s;219 testes Node194.189s;144 testes Go de topo com race507.918s (11 helpers executados pelos drivers);30 casos de interoperabilidade270.997s; fronteira SQLite C115.655s;17 UI Node115.990s e17 UI Go110.472s.22 testes host iOS1.887s e estática0.030s. Desktop Linux: preparação0.233s, execução1.041s, pacote5.812s, execução empacotada0.800s.26 relatórios Axe actualizados, zero violações.277 ficheiros de fonte inalterados durante os gates. Evidência em docs/evidence/group-send/final. [GROUP-SEND](GROUP-SEND.md) delimita o resultado. Confirmações automáticas, eventos, carriers e composição/gestão dinâmica na UI continuam pendentes; não há novo gate móvel.

**CI1663cbe — run34754740869 terminado:** Node3OS, Go e três desktop passaram. WebKit real aceitou a nova política e recusou a anterior com WKErrorDomain:6;45 asserções de host passaram.1 XCTest de arranque passou33.838s e a importação da fotografia no simulador38.204s. O teste funcional criou a identidade pela UI, mas saiu65 em119.017s:1 caso falhado74.523s ao esperar que o formulário de publicação fechasse; nenhum fluxo completo passou. Capturas/relatório em [evidence/ios/1663cbe](evidence/ios/1663cbe). O log sanitizado identifica post form completed; a primeira listagem omitiu *.log e foi corrigida. A causa ainda não está determinada; não a atribuir à posição dos campos pela imagem inicial.

**CI1aaca64 — run34752751771:** Node3OS, Go completo e três jobs desktop passaram. O novo teste iOS de arranque falhou antes da fotografia; a captura nativa mostra falha ao activar o isolamento, no ramo de compilação das regras WebKit. Boot169.530s/build-for-testing39.092s/install13.165s passaram, teste de arranque saiu65/165.729s. Nenhum fluxo funcional iOS passou. [Relatório e captura](evidence/ios/1aaca64); o commit1663cbe separa origem exacta/prefixo com barra e acrescenta testes com compilador WebKit real. No CI34754740869 passaram os testes Swift/WebKit e build app/framework iOS; o teste funcional no simulador continua pendente.22 testes host do runner e a verificação estática passaram localmente. A política de isolamento não foi relaxada. [Âmbito local](evidence/ios/webkit-rule-syntax).

**Em implementação. Experimental. O contrato completo não está concluído.** Não é infraestrutura validada para catástrofes; não substitui serviços de emergência. Actualização: 2026-09-13. O CI verde de c3f5b42 e os gates posteriores de outbox/SAF têm evidência e versões distintas; os novos marcos não herdam automaticamente os passes CI anteriores.

**CI93c598f — run34750795247:** Node passou nos três OS. Build, testes/race, C SQLite, interoperabilidade e UI do job Go constam como success, mas o job foi cancelado ao atingir15 minutos; a anotação do GitHub confirma o limite global. Desktop/iOS foram skipped, sem resultado novo. O orçamento desse job passa a25 minutos com todos os passos e limites internos preservados; [evidência e comparação YAML](evidence/ci-93c598f). Isto não é HTTP408 do upstream.

**Integração local de autoridade da outbox, posterior a fde529e:** Node/Go reconciliam paragens no commit de membros, recuperam mirrors autenticados, preservam reservas/pins e retiram futuros pacotes locais. Inventário/pedidos verificam a autoridade do autor, incluindo envios completos. A interface apresenta pausa/interrupção; os percursos dirigidos Node/Go e Axe desktop/móvel passaram. As fixtures ainda instalam o bundle e a intenção inicial: publicação dinâmica, confirmações emitidas pela app, carriers e UI de composição dinâmica continuam pendentes. A regressão completa desta árvore passou: 211 testes Node passaram179.064s;141 testes Go de topo com race476.758s (10 helpers omitidos isoladamente e executados pelos drivers);26 casos de interoperabilidade243.460s; fronteira SQLite C75.323s;17 UI Node115.845s e17 UI Go109.616s. Build5.524s;22 testes iOS host1.842s e verificação estática0.032s. Desktop Linux: preparação0.336s, execução1.507s, pacote10.029s e execução empacotada1.280s.26 relatórios Axe actualizados, zero violações. Fontes inalteradas durante os gates. Evidência em `docs/evidence/group-outbox/final`; [GROUP-OUTBOX](GROUP-OUTBOX.md) delimita a evidência.

**CI terminado de fde529e — run34728934069:** Node3OS, Go e três pacotes desktop passaram. iOS26.4.1 compilou/arrancou/instalou, mas a fotografia voltou a exceder o prazo em60.836s; XCUITest não executou. Diagnóstico em5.754s, sem causa demonstrada. [Evidência](evidence/ios/fde529e). A captura da falha de arranque continua por executar. A alteração seguinte verifica o arranque antes da fotografia, mantendo o gate completo;22 testes host e verificação estática passaram, sem compilação/execução Apple.

**Marco de reservas/admissão posterior a274004e:** os dois núcleos têm admissão transaccional, quarentena física e consulta de histórico por épocas. O gate completo passou: 190 testes Node passaram137.762s;129 testes Go de topo/race396.073s (9 helpers executados pelos drivers de interoperabilidade);24 testes de interoperabilidade219.736s; fronteira SQLite C15.538s;16 UI Node112.941s e16 UI Go103.288s. Build6.072s e CLI0.221s passaram. Desktop Linux: preparação0.224s, execução1.028s, pacote5.347s e execução empacotada0.798s.22 relatórios Axe actualizados, zero violações. Fontes inalteradas em todas as fases. Evidência em docs/evidence/group-content/final. [GROUP-CONTENT](GROUP-CONTENT.md) delimita o comportamento. Envio/outbox/carriers/UI dinâmica ainda não estão completos; as fixtures publicam os bundles de teste.

**Diagnóstico iOS posterior, apenas host:** captura adicionada antes do teardown em caso de falha do arranque; preserva o erro e o prazo.19 testes host e verificação estática passaram, sem compilação Swift nem execução Apple. Evidência em [evidence/ios/startup-capture-host](evidence/ios/startup-capture-host).

**CI terminado de274004e — run34721962376:** Node nos três OS, Go e os três pacotes desktop passaram. A fotografia iOS passou em10.038s. O XCUITest lançou a app e executou1 teste, que falhou em57.965s ao esperar45s pela WKWebView; nenhum fluxo funcional passou. Evidência em [evidence/ios/274004e](evidence/ios/274004e). Nenhum passe Apple é atribuído a este marco. O registo está em `.cache/ci-274004e-run.json`.

**CI mais recente observado — bff00cc, run34697388472:** Node em Windows/Linux/macOS, Go completo e três pacotes desktop passaram. iOS26.4.1 falhou em `seed-synthetic-photo` depois de funcionar o pré-requisito Node; XCUITest não executou. Resultado observado em [evidence/ios/bff00cc/observed-ci.json](evidence/ios/bff00cc/observed-ci.json); logs originais na cache do projecto. Não repetir a preparação inalterada nem aumentar prazos. A versão local posterior não herda automaticamente estes resultados.

**Integração local de grupos, posterior a esse CI:** política de acesso, registo persistente e API de gestão em Node/Go implementados, com quatro percursos dirigidos passados em 5,684 s. Gestão não é mensagem dinâmica: faltam admissão/retenção/outbox, sincronização P2P de controlo e UI. Gate completo: build5.497s;177 testes Node122.851s;118 testes Go de topo com race380.032s (oito helpers omitidos sem fixture e exercitados pelos drivers reais);20 casos de interoperabilidade199.504s; driver C dirigido8.039s;16 UI Node112.105s e16 UI Go106.110s. Desktop Linux: preparação0.239s, execução2.075s, pacote5.377s e execução empacotada0.795s.22 relatórios Axe actualizados sem violações. Fontes inalteradas em todas as fases. Evidência em `docs/evidence/group-runtime/final`. Capturas do editor/social Node e conversa escura Go revistas por root; isto não é revisão independente. Nenhuma nova execução móvel pertence a este gate. [GROUP-RUNTIME](GROUP-RUNTIME.md) delimita comportamento, controlos e trabalho pendente.

**Android actual após correcção de compatibilidade:** APK136a5103c82c4c87f6865aa2b68daa5eb60796ac4d420aa8f09d5fea5fd4e2a5, com AAR9e2fb77f9938721c9df7ef82df19e357ab02ff0c417ec40061a82e0f27004585:82 asserções no mesmo emulador API36x86_64 (38 SAF36.693s,15 prazo134.729s,16 mensagens15.355s,13 relay18.913s) e inspecção cifrada0.768s passaram. Hash instalado e inputs inalterados; cofre/JSON legado preservados; SQLite Android e binding autenticados pelo Node. Root reviu a captura nativa final. Instrumentação removida, forwards vazios, AVD/adb próprios parados, identidade preservada. Evidência em `docs/evidence/android-sqlite/apk-136a5103`. Não é hardware físico nem validação de catástrofes. O driver C passou ainda78 Go de topo com race,16 interoperabilidade e15 UI no Linux; o backend dos outros alvos continua pure Go. Esse APK não contém a nova gestão por épocas da API; mensagens dinâmicas/UI, keystore/rotação e restantes requisitos continuam pendentes.

**Checkpoint remoto observado — 90cb649, CI34687481472:** Node passou Linux/macOS/Windows, Go completo passou e os três pacotes desktop passaram. iOS26.5 arrancou o simulador, compilou para teste e instalou a app; `seed-synthetic-photo` excedeu o prazo em60.651s, antes do XCUITest. Diagnóstico recolhido em2.947s; só o simulador criado foi apagado. Os registos mostram reconstrução da fototeca, erros de atributos em imagens predefinidas do simulador e estados posteriores de prontidão, sem provar a causa do bloqueio de `addmedia`. Execução da app iOS continua bloqueada; não é timeout Copilot. Evidência exacta em `evidence/ios/90cb649`.

**Integração privada local verificada, posterior ao commit publicado:** ambos os núcleos importam agora estado privado no SQLite cifrado, com ligação assinada por instalação e recuperação de falhas sem fallback ao JSON antigo. Gate dirigido de API Node↔Go:4 casos passaram em4.359s, incluindo migração, morte de processo, preservação das alterações e recusa de estado ausente/corrompido. Gate completo concluído: build/typecheck,152 Node/77.381s,105 testes Go de topo com race/379.168s (cinco helpers omitidos em unitários e executados pelos drivers),15 interoperabilidade/145.127s,15 UI Node/107.327s e15 UI Go/101.522s. Desktop Linux: preparação0.239s, execução0.982s, pacote5.161s, execução empacotada0.766s.22 relatórios Axe actualizados, zero violações. As fontes registadas não mudaram durante o gate. Evidência pública em `docs/evidence/private-profile`; produto, grupos dinâmicos e plataformas móveis continuam incompletos. A autoridade de grupos ainda não está ligada à admissão/outbox/API/UI. Os artefactos móveis anteriores não incluem esta mudança.

**Checkpoint remoto anterior — 0289ec7, CI34675214610:** passaram os jobs Node nos três OS, Go completo e os três pacotes desktop. O iOS26.5 compilou, arrancou o simulador e instalou a app, mas a preparação da fotografia voltou a terminar por timeout (61,431s), antes do XCUITest. O diagnóstico limitado recolheu a inicialização/reconstrução da fototeca e estados posteriores de prontidão; não isolou conclusivamente a causa. A limpeza eliminou apenas o simulador criado. Não há execução da aplicação iOS demonstrada. Evidência em `docs/evidence/ios/0289ec7`; não repetir esta preparação inalterada nem aumentar prazos para obter um resultado verde. As secções seguintes preservam o histórico de versões anteriores.

**CI posterior — b86a5b1, run34686129024:** Node passou nos três OS. Go passou build/compilação/unitários/race, mas dois testes de interoperabilidade falharam ao gravar relatórios numa pasta inexistente no runner limpo. UI Go, desktop e iOS foram omitidos. O erro foi reproduzido com fontes publicadas e corrigido criando a pasta nos próprios testes; ambos passaram isoladamente em cache limpa, sem alterar produção ou cobertura. O CI seguinte90cb649 passou estes casos e o restante job Go. Ver `CI-B86A5B1-FOLLOWUP.md`.

**Implementação local posterior:** armazenamento transaccional cifrado e índice assinado para metadados de grupos em Node e Go, ainda sem importação na aplicação. O gate integrado passou104 Node,76 Go de topo com race e7 casos de interoperabilidade. Uma correcção posterior do filtro de esquema SQLite foi reproduzida antes e validada depois: build,12 testes de armazenamento Node,10 Go com race e interoperabilidade real passaram. Os motores partilham ficheiros cifrados, preservam24 incrementos concorrentes e recuperam a transacção interrompida pelo outro processo. Não é execução móvel. Limites, versões, comandos e evidência em `GROUP-STORAGE.md`; as contagens da suite completa precedem a última correcção, que tem gates próprios. Grupos dinâmicos, sincronização de provas e a sua UI continuam pendentes.

Antes do próximo CI, o runner iOS passou a recolher, em até15s e dentro do prazo global/reserva originais, os registos da fototeca do simulador criado caso a preparação da fotografia falhe. Não reinicia serviços nem altera permissões/prazos; conserva a falha original e a limpeza.13 testes host, verificação estática, typecheck e a interoperabilidade de armazenamento passaram depois desta mudança. É instrumentação para diagnóstico, sem evidência adicional de execução Apple. Ver `docs/evidence/ios/photo-diagnostics-host`.

## Evidência inicial neste Linux (histórico)

| Comando / artefacto | Resultado observado | Alcance |
| --- | --- | --- |
| `npm run build` | Passou | TypeScript + interface de produção Vite |
| `npm test` | 79 testes passaram, 0 omitidos, 45,4 s | Cofres/assinaturas/ACL, armazenamento, API, processos, TCP/série PTY, consentimento, colecções, snapshots limitados, políticas desktop e simulação identificada como tal |
| `npm run test:e2e` | 14 percursos passaram após a revisão da outbox | Dois browsers/daemons reais: identidades, contactos, TCP, mensagens, anexo descarregável, reacção, grupo, social, pesquisa, isolamento de rascunhos, bloqueio com resposta atrasada, paginação, editor/rascunho cifrado, página remota com autor desligado e recuperação do nó |
| `node scripts/e2e.mjs tests/e2e/flows.spec.ts --reporter=line --output=.cache/drag-ui-e2e` | Passou, 18,4 s | Repetição posterior com dois movimentos drag/drop reais, além das alternativas de teclado/setas e do fluxo P2P completo |
| `docs/evidence/heterogeneous.json` | Passou | 3 processos; A→B TCP, B→C série PTY; C sem ouvinte TCP; partição, recuperação, pausa e substituição do autor desligado pelo seeder |
| `docs/evidence/ui/*-axe.json` | Zero violações nos estados capturados | Onboarding, conversa clara/escura, social, editor e social a 390 px; tags WCAG 2 A/AA e 2.1 AA. Não é certificação de acessibilidade |
| `docs/evidence/ui/*.png` | Capturas reais | Conteúdo fictício criado pelos testes, sem dados do proprietário |
| Revisão independente de agentes | Realizada para segurança, transportes e design | Falhas concretas corrigidas e testes repetidos; não é auditoria externa de segurança |

Algumas primeiras execuções falharam; as causas/correcções estão em `.codex-delivery/learning-log.md`. O navegador integrado não encontrou backend; os e2e usam Chromium do projecto, com sandbox activado, em Linux. Um viewport móvel não prova execução Android/iOS.

## Funcionalidades

| Pedido | Implementado/testado | Parcial, por implementar ou verificar |
| --- | --- | --- |
| Identidades e chaves | Ed25519, X25519/HKDF/AES-GCM, prova do cartão, cofre scrypt/AES-GCM, exportação/recuperação | Keychain nativa, rotação/revogação completa, ratchet/forward secrecy, auditoria criptográfica externa |
| Mensagens 1:1 e grupos | Texto, destinatários explícitos, grupos fixos, respostas, reacções, pesquisa, edição/eliminação pelo autor; gestão dinâmica pela API Node/Go | Mensagem dinâmica, carriers, admissão/outbox e UI por integrar; nova outbox distingue recepção/leitura por destinatário e expiração, com os limites documentados abaixo |
| Anexos | Bytes cifrados, ficheiros, renderizadores imagem/áudio/vídeo; imagem/áudio/vídeo/ficheiro verificados por UI e bytes entre pares; 108 kB na rota heterogénea | Gravação de voz implementada/testada com entrada Web Audio sintética e MediaRecorder real; microfone físico/câmara não testados. UI até 2 MB/anexo, objecto até 4 MiB |
| Persistência | Intenção de envio cifrada/idempotente enquanto retida, reserva limitada, repetição própria após restart mesmo com relay pausado, estados por destinatário/expiração, metadados autenticados locais e rascunho de página cifrado | Rascunhos de mensagens por conversa ficam em memória; disponibilidade depende de quota/TTL |
| Social: perfil, feed, posts | Página pessoal assinada, posts públicos/privados, feed local verificado | Sem descoberta global, recomendações ou sincronização nativa em segundo plano |
| Social: seguir | Preferência local persistente e filtro A seguir verificado | Relação pública assinada/followers ainda ausente |
| Social: comentários/reacções | Publicação e verificação; fluxo real entre clientes | Ricos controlos de gestão/edição de comentários incompletos |
| Social: colecções/partilhar | Guardados, colecções nomeadas cifradas, criar/renomear/eliminar/adicionar/remover; obtenção pelo endereço sem conceder leitura | Colecções sociais partilhadas/federadas não implementadas |
| Moderação | Bloqueio local, denúncia local, tombstones assinados e memória local autenticada | Sem federação de denúncias, sem moderação central; cópias remotas não podem ser recolhidas |
| Armazenamento/seeding | Verificação em recepção/leitura, quotas, 1.024 objectos, pin/unpin, TTL, LRU, seeder sem autoridade de autor; estado paginado até 100 resumos/4 MiB e anexos sob pedido | Journal privado limitado a 16 MiB; cache de resumos limitada a 16 MiB/1.024 entradas; leitura inicial de stores grandes ainda tem custo de verificação e cifra. Não é protecção absoluta contra exaustão |
| Encaminhamento | TCP+série, fragmentação, SOS antes de bulk, justiça, ACK/retry, hop/TTL, duplicados, limites e pausa consentida | Flooding limitado, sem optimização de rotas; retry de pacotes em memória e inventário de objectos persistente |
| Página pessoal | Blocos seguros, paletas, reordenar drag/drop/setas/teclado com browser real, pré-visualizar, guardar rascunho, publicar/ler offline | Dispositivo táctil físico e galeria/template/media mais rica por completar |
| Emergência | Saúde e pares reais, assinatura vs exactidão do alerta, SOS, baixo consumo | Não validado em catástrofes, ensaios de rádio/alcance ausentes |
| Acessibilidade/design | Light/dark, reduced-motion, contraste automático, teclado do editor, revisão de imagens | Leitor de ecrã real, dispositivos tácteis físicos e todas as combinações de estados ainda não testados |
| Notificações | Adesão explícita no browser, texto genérico, coalescência, limpeza ao bloquear; três e2e com API substituída | Notificações reais do OS, dispositivos e entrega em segundo plano não verificadas |

## Plataformas e meios

| Alvo | Estado honesto |
| --- | --- |
| Linux | Node 22.22.3 e Chromium executados nativamente; artefactos/testes acima. Shell Electron e pacote Linux x64 descompactado executados; fluxo de instalador ainda não testado |
| Windows | Node e pacote desktop Windows x64 passaram no runner Windows em bff00cc. Testes PTY omitidos. GUI, instalador e hardware físico não executados |
| macOS | Node, PTY e pacote desktop macOS arm64 passaram no runner Apple em bff00cc. GUI/instalador desktop, assinatura de distribuição e hardware Apple local não testados |
| Android | APK `136a5103…`: 38 asserções SAF,15 de prazo/lifecycle,16 de mensagens/recuperação,13 de relay/seed e captura nativa, mais inspecção Node do SQLite Android.82 asserções passaram no mesmo emulador API36x86_64, com hashes e identidade preservados. A API nova de grupos exige rebuild/gates próprios; não é dispositivo físico, ARM64 nem rádio |
| iOS | Em274004e, Xcode26.6/iOS26.4.1 compilaram/arrancaram o simulador e importaram a fotografia. XCUITest lançou a app;1 teste falhou ao esperar a WKWebView. Não há fluxo funcional iOS passado; Go/web UI a arrancar com êxito, dispositivo físico e assinatura continuam por verificar |
| TCP | Sockets/processos reais locais; LAN/Internet/NAT ainda não verificados |
| Série | Adaptador real + PTYs do sistema; nenhum rádio físico validado |
| BLE/Wi-Fi Direct/LoRa | Não implementados/testados |
| Reticulum/RNS | Documentação e licença avaliadas em TRANSPORT.md; nenhuma compatibilidade implementada/alegada |
| Simulação | Motor separado, sementes fixas e controlos; não representa hardware ou OS alvo |

## Próximos marcos

1. Concluir a regressão local de política/registo/API de grupos, registar o resultado e publicar marcos verificados. A execução mantém-se sequencial, sem agentes novos/retomados.
2. Ligar admissão, retenção de bytes, pins, quarentena e paragens da outbox na mesma transacção. Exibir/confirmar/transmitir apenas depois do commit. Implementar carriers e sincronização limitada, com adesão explícita, e depois UI de grupos dinâmicos.
3. Executar os controlos novos de rede/partição/heal/multi-adapter/seeder, autorização/corrupção e interface; recompilar os artefactos móveis correspondentes e repetir os gates nativos disponíveis. O bloqueio iOS actual é a ausência da WKWebView após lançamento real; a fotografia já passou. Recolher a tela nativa de falha antes de atribuir a causa interna.
4. Continuar keystore/rotação, pesquisa integral, social/media/templates, notificações reais, acessibilidade e revisão independente quando autorizada, mantendo todos os requisitos e limitações de hardware/assinatura.

Continuidade e 80+ critérios normalizados: `.codex-delivery/implementation-plan.md`, `requirements-normalized.json`, `traceability.md`. Não reduzir o objectivo ao marco já implementado.

## Evidência adicional e limites de versão

`docs/evidence/ci-919beec.json` identifica o commit e jobs efectivamente observados (CI do núcleo Node, não da aplicação Go/Android em desenvolvimento). `docs/evidence/desktop` regista arranque do Electron e do binário Linux empacotado, API401/200, isolamento e encerramento. `docs/evidence/media` identifica entrada sintética e codificação/transporte reais. `docs/evidence/notifications` usa sempre APIs de notificação substituídas, sem alegar que apareceu uma notificação do OS.

O núcleo Go passou 11 testes e o transporte 11 testes com detector de corridas; a aplicação passou 17 testes normais e 17 com detector de corridas após a revisão de cache/paginação/tombstones. O gate Node↔Go compara 1.500 vectores numéricos, Unicode/UTF-16, cofres/chaves/ACL/cifra e armazenamento nos dois sentidos. As duas integrações mistas passaram após recompilar o CLI final (`a3dc0344…`): Go→Node→Go TCP e Go→Node→Node TCP+série PTY. A aplicação Go não implementa driver série nativo. Estes testes estão separados dos 66 testes Node acima.

A resposta periódica de um store Go cifrado de 37,6 MB ficou em 68 KB e o endpoint de anexo devolveu 2 MiB exactos. O primeiro processamento completo sob race detector levou 22,54 s; o seguinte, com cache verificada, 254 ms. São medições deste host instrumentado, não uma promessa de latência móvel. Ver `APPLICATION-REVIEW.md` para os controlos e limites, incluindo eventos privados recebidos enquanto bloqueado.

## CI publicado e integração local posterior

O run GitHub `34655608855`, commit `c3f5b42ff80923981befecec1f82e3ff017bd6a3`, terminou com sucesso nos8 jobs: Node em Linux/Windows/macOS, núcleo/aplicação Go e UI, três pacotes desktop e compilação iOS. `docs/evidence/ci-c3f5b42.json` contém os jobs efectivamente observados. O artefacto iOS foi descarregado e inspeccionado em `docs/evidence/ios/c3f5b42`: Xcode26.6(17F113), Go1.26.8, framework de dispositivo+simulador arm64, app de simulador SHA-256 `9a8cf91304108fe70259053104dd9fd3752a99ad656b1c0f5eea728f39337904`. É compilação real, não execução da aplicação no simulador. Linux CI também executou o desktop e o pacote descompactado com sandbox/Xvfb.

A supervisão do proprietário observou uma falha local de contactos/resposta de grupo durante alterações concorrentes (`TestApplicationMessagesGroupsReceiptsAndPrivacy`, app_test.go117). Isto não é uma regressão demonstrada do commit remoto verde. A aprendizagem de cartões foi corrigida sem reduzir ACL/cobertura; o teste focado e os gates locais estabilizados passaram, como documentado abaixo. Os resultados posteriores têm hashes próprios e não substituem silenciosamente os anteriores.

## Outbox: evidência local posterior a c3f5b42

As alterações Node/Go da outbox passaram79 testes Node (incluindo13 regressões novas),36 testes Go de aplicação normais e com race detector, e2 cenários de3 processos mistos em82,890s. O hash das fontes de produção foi verificado inalterado durante os testes e novamente na integração: `66a18d61cc0a63b0bf30195462c2515912b333614d95dd69f1d503db409f830e`. CLI Go: `4e18b8b15d0da0883586a4d3e51e1ecbec351afd1ce1545bcb773d8f8aca422d`. O teste de grupos/contactos indicado pela supervisão foi corrigido e está incluído nos36, sem redução de ACL/cobertura. Os11 testes core e11 transport Go foram repetidos com `-race -count=1 -p=2`, passando em5,902s e9,136s.

A interface passou14 percursos Node e14 Go; a revisão posterior do aviso de repetição dentro do diálogo passou os4 percursos de outbox em cada núcleo. Esses últimos processos já tinham sido iniciados antes da instrução de verificação sequencial do proprietário. Foco de teclado, leitura parcial de grupos antigos, expiração e repetição após resposta perdida têm testes/screen captures reais. A última cópia da interface usa `index-Ct0M5tYK.js`/`index-BcajBo5I.css`.

A outbox conserva128 envios pendentes/32MiB e até256 registos totais. A idempotência é limitada aos registos retidos; a interface não repete silenciosamente uma operação incerta que já não esteja no estado fresco e desbloqueado. Confirmações assinadas são independentes de ACKs do relay; recibos antigos, dados evictos e grupos com criador bloqueado são cobertos. Atomicidade sincroniza ficheiro/directório emPOSIX; não é ensaio de corte físico de energia e não promete flush de directório noWindows. Ver `OUTBOX-PROTOCOL.md`, `OUTBOX-REVIEW.md`, `NATIVE-OUTBOX.md` e `docs/evidence/outbox`.

Verificação operacional sequencial pedida pelo proprietário em 2026-09-12: os agentes activos terminaram com entregas reais, sem novas tasks/retomas/interrupções. Foi executado apenas `node scripts/e2e.mjs tests/e2e/outbox.spec.ts --grep 'durable composer retries a lost response once' --reporter=line --output=.cache/sequential-stability/outbox`: **1 teste passou em 11,7 s**, uma execução, um worker, zero repetições, saída 0 e fontes/assets inalterados. As quatro auditorias Axe desse percurso tiveram zero violações. Resultado, saída e capturas: `docs/evidence/sequential-stability/2026-09-12`; contexto e limites: `.codex-delivery/SEQUENTIAL-CHECK.md`. Não foi observado novo 408 nesta etapa; isso não determina a causa dos anteriores. Não houve outro teste/build/push nesta medição; o contrato e o harness mantêm-se.

## Integração Android posterior à medição

Em fase posterior, mantendo execução sequencial e sem novos agentes, root compilou o APK `27a71947f73e3a2622da6efbcb402d04260e53293cb6156e27f43ea3b3f8e5a2` com o checker/interface finais. O hash instalado coincidiu antes/depois; SAF38, prazo15, mensagens16 e relay11 passaram, uma execução de cada gate e fontes/assets/AAR inalterados. Prazo observado121.264ms, HOME1.254ms; bytes exactos e recuperação da identidade confirmados. Os controlos de ausência de rota, pausa/heal e seeder com autor desligado passaram. Três capturas foram revistas. Evidência: `docs/evidence/android/documents-27a71947`, comandos completos em `ANDROID.md` e `.codex-delivery/ANDROID-FINAL-INTEGRATION.md`. AVD/adb próprios parados, instrumentação de teste removida, identidade preservada,127,54GiB livres. Isto fecha o gate do checker que estava pendente no APK4de, sem alegar hardware físico, rádio ou suspensão real.

## CI de 3d6641a e correcção em curso

Os cinco marcos foram publicados normalmente até `3d6641a`. O run `34663513261` passou Node nos três OS e os testes unitários/race Go, mas falhou o quinto caso de interoperabilidade: a outbox Go podia mostrar expiração enquanto a reserva automática ainda estava fixada. Desktop e iOS foram omitidos por dependência, não executados com sucesso. Root reproduziu o problema também em Node e corrigiu a inconsistência de instantes entre reconciliação e resposta. Os controlos focados de estado/envio/repetição passaram; gates completos estão em curso antes de publicar a correcção. Ver `OUTBOX-SNAPSHOT-EXPIRY.md`. Os binários anteriores mantêm a sua evidência por hash e precisam de rebuild para incluir esta correcção.

Há ainda uma biblioteca Node local de certificados de grupos, com11 testes criptográficos passados e typecheck, mas sem integração na aplicação nem port Go. Grupos dinâmicos continuam por implementar/testar de ponta a ponta; os casos declarativos originais não foram transformados automaticamente em resultados executados.

## Marcos correctivos posteriores

A correcção de expiração ficou em `7246340`: build,93 testes Node (82 da aplicação/existentes e11 da biblioteca de grupos),37 testes Go de aplicação com race,11 core/11 transport e5 casos de interoperabilidade passaram. O cenário que falhou no CI passou mantendo a asserção original. Hashes e comandos: `docs/evidence/outbox/snapshot-expiry`. O layout ficou em `c7f5488`:15 percursos UI por núcleo, oito larguras, teclado, contraste claro/escuro e execução real do desktop/pacote Linux passaram; o smoke passou a rejeitar overflow horizontal. Ver `ONBOARDING-LAYOUT-REVIEW.md`.

O Android foi novamente ligado/compilado para incluir as correcções: AAR51dedfe0 e APKfa1481d3 passaram os quatro gates,80 asserções no mesmo emulador, com hashes/inputs preservados. Evidência: `docs/evidence/android/documents-fa1481d3`. O núcleo Apple ainda precisa de nova compilação/execução CI; não herda os passes locais. Existe um rascunho Go da biblioteca de grupos em desenvolvimento, ainda sem testes ou integração na aplicação; a funcionalidade de grupos dinâmicos permanece parcial.

## Resultado CI f93e741 e biblioteca de certificados

O run34669716466 confirmou Node nos três OS, todo o job Go e pacotes Windows/macOS. Linux desktop falhou na captura do compositor (`UnknownVizError`); iOS falhou no limite de180s enquanto o simulador criado ainda migrava dados iniciais. A app iOS não foi executada; a limpeza eliminou só o simulador criado. `CI-F93E741-FOLLOWUP.md` descreve a captura por frame apresentado e o novo limite finito de600s de primeiro arranque, com controlos de segurança/disco e prazo global preservados. Smoke local e11 testes host do runner passaram; a recuperação no CI está por observar.

A biblioteca de certificados tem agora implementação Node e Go,11 casos Node,8 testes Go com race e um vector bidireccional executado em processos reais, incluindo assinaturas negativas válidas sob a chave sem autoridade, Unicode exacto e envelopes de leitores. `GROUP-CERTIFICATES.md` delimita esta evidência. Registry durável, sincronização de provas, admissão/quarentena de mensagens e UI de grupos dinâmicos continuam por integrar; não há suporte completo de grupos dinâmicos.

## Registo de autoridade local após manutenção

O registo Node em desenvolvimento executa criação/convites/consentimento, alterações pelo criador, saída local, encerramento, conflitos assinados, recuperação de capacidade e consulta finita de operações, sobre SQLite cifrado real. Testes existentes de dois processos verificam CAS, morte antes do commit e perda de resposta após commit. A revisão local acrescentou protecções de estado privado intermédio, registos persistidos e suspensão por quota. O helper de certificados Go passou9 testes com race, saída0, com resultado recuperado por hashes após manutenção. Os gates Node finais desta alteração estão em curso; a versão Go do registo e a integração na aplicação/API/UI continuam pendentes. Não é ainda suporte de grupos dinâmicos no produto.

## Exclusividade do perfil após integração local

Node e Go já adquirem exclusividade do perfil antes de ler dados mutáveis, recusam uma segunda instância cooperante e libertam o bloqueio após fecho/morte do proprietário. Build,18 casos Node de runtime/outbox,2 casos Go com race, reconstrução do CLI e dois testes reais de aplicação Node↔Go passaram; a identidade e a operação pendente são preservadas. O gate alargado passou133 Node e continua por Go/interoperabilidade/UI/desktop. Os APKs/artefactos Apple anteriores não incluem esta mudança. `GROUP-AUTHORITY.md` distingue este controlo das bibliotecas de grupos ainda sem integração de produto e documenta a lacuna de reserva no limite próprio do índice, ainda em investigação.

## Gate da integração de propriedade do perfil e correcção do índice

O gate congelado em `.cache/authority-runtime-gate` terminou:133 Node (74.563s),95 Go de topo com race (353.956s; quatro helpers omitidos no modo unitário e executados pelos drivers),11 cenários de interoperabilidade (134.734s),15 UI Node (113.698s) e15 UI Go (102.264s) passaram. Desktop Linux foi preparado, executado, empacotado e executado a partir do pacote, com controlos de segurança preservados. Root reviu a imagem de arranque a720px e o fluxo social móvel; os18 relatórios Axe UI/outbox actualizados tiveram zero violações. Leitor de ecrã físico e execução em hardware continuam não demonstrados.

Uma correcção posterior resolveu a falta de reserva no limite próprio do índice cifrado:14 Node de armazenamento/índice,11 Go de armazenamento com race e os dois testes de partilha de ficheiros/processos passaram. O controlo com18 717 registos e uma variante Go sem a protecção falha exactamente onde deve. Fonte de produção intacta no controlo negativo; nenhuma ponte, autenticação, serviço ou política de segurança alterada. `GROUP-STORAGE.md` e `GROUP-AUTHORITY.md` delimitam as versões e evidências; a verificação final da autoridade também passou25 Node (23 autoridade+2 índice),12 Go com race e o teste real de interoperabilidade, com evidência própria em `evidence/group-authority/final`. Grupos dinâmicos, sincronização de provas e integração transaccional da outbox/API/UI continuam pendentes.


A facade transaccional Node/Go foi implementada e verificada:81 Node/36.565s,36 Go de topo com race/169.094s (quatro helpers executados pelos drivers),9 interoperabilidade/45.760s; build8.157s e CLI0.901s. Fontes registadas inalteradas durante o gate. Evidência em `docs/evidence/group-transaction`. A aplicação ainda não usa esta facade para grupos dinâmicos.


## Preparação Apple e polimento posterior

A próxima CI pede explicitamente iOS26.4.1, que constava do inventário instalado de90cb649, como tentativa limitada de compatibilidade após o bloqueio de26.5.17 testes host e verificação estática passaram; a fotografia/XCUITest e os prazos continuam obrigatórios. Nenhuma execução Apple nova é inferida. Ver `IOS-SIMULATOR.md`.

O JS da interface passou a `index-C9JlHp94.js` (CSS inalterado): singulares corrigidos, nomes dos blocos em PT e explicação do editor simplificada. Os dois percursos existentes de fluxos completos/layout passaram em Node19.582s e Go19.209s; o desktop Linux foi reconstruído/empacotado/executado em0.200/5.047/0.841s. Root reviu as capturas actuais de editor e feed móvel. Evidência em `evidence/ui/copy-polish`. Isto é posterior ao gate completo de15 UI por núcleo da migração; o APK anterior ainda não inclui estas alterações.
